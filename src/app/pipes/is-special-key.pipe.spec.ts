import { IsSpecialKeyPipe } from './is-special-key.pipe';

describe('IsSpecialKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSpecialKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
